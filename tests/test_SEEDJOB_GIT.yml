version: "3.5"
services:

  gitserver:
    build: fixtures/git

  jenkins:
    image: ${IMAGE_NAME}
    ports:
      - 8080
    depends_on:
      - gitserver
    environment:
      SEEDJOB_GIT: git://gitserver/data
      